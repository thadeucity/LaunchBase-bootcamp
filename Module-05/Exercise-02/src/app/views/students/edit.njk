{% extends "layout.njk" %}

{% block content %}

<div class="card">

  <section class="avatar" style="background: url({{student.avatar_url}}) no-repeat center center / cover"></section>
  <section class="info">
  
    <h3>Edit Student</h3>

    <form class="student-form" method="POST" action="/students?_method=PUT">

      {% include 'students/form.njk'%}

      <input 
        type="hidden"
        name="id"
        value="{{student.id}}"
      >

      <div class="btn-grid">
        <button class="save-btn" type="submit">Save</button>
      </div>

    </form>

    <form id="form-delete" class="delete" method="POST" action="/students?_method=DELETE">

      <input 
        type="hidden"
        name="id"
        value="{{student.id}}"
      >

      <div class="btn-grid">
        <button class="delete-btn" type="submit">Delete</button>
      </div>

    </form>


  </section>

</div>

{% include 'confirm.njk'%}

{% endblock content %}